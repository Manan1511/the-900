To prevent client-side API key exposure, all sensitive API calls should be routed through a secure backend proxy. The frontend should make requests to your own server, which then securely calls the Gemini API using the stored API key (e.g., in server-side environment variables). This keeps the API key hidden from the client. Additionally, replace the client-side `alert()` with a more subtle UI notification or logging mechanism.