For rich text editing, consider using a well-established and security-audited rich text editor library (e.g., TinyMCE, Quill, Draft.js, TipTap) that properly sanitizes HTML input and handles content rendering. These libraries are designed to manage the complexities of `contentEditable` securely. If only plain text input with simple formatting is required, replacing `contentEditable` with a `<textarea>` and implementing formatting through Markdown-like syntax or custom text manipulation would be a safer and more maintainable approach.